# Cron schedule for the sync (default: hourly).
CRON_SCHEDULE=0 * * * *

# Host directories bind-mounted into the container.
# LIBRARY_DIR holds the per-person source folders (read-only),
# DEST_DIR is the share the photos are copied into.
LIBRARY_DIR=./example/library
DEST_DIR=./example/dest
